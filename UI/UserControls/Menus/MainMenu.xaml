<UserControl x:Class="UI.UserControls.Menus.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.UserControls.Menus"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Menu HorizontalAlignment="Left" Style="{DynamicResource SimpleMenu}" VerticalAlignment="Top" Margin="0,0,0,0">
            <MenuItem Header="File"     Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
            <MenuItem Header="Edit"     Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
            <MenuItem Header="View"     Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
            <MenuItem Header="Project"  Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
            <MenuItem Header="Build"    Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
            <MenuItem Header="Debug"    Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
            <MenuItem Header="Window"   Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
            <MenuItem Header="Help"     Margin="0" Style="{DynamicResource SimpleMenuItem}"/>
        </Menu>
    </Grid>
</UserControl>
